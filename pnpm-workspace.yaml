packages:
  - "apps/*"
  - "packages/*"

# Keep native optional dependencies reproducible across local Windows and Linux CI.
supportedArchitectures:
  os:
    - win32
    - linux
  cpu:
    - x64
  libc:
    - glibc
